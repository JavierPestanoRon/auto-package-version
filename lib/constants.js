const appRoot = require('app-root-path'); // project path

export const projectJsonPath = `${appRoot}\\package.json`; // project package.json path
export const backupFolderPath = `${appRoot}\\json_versions`; // package.json backup folder path
